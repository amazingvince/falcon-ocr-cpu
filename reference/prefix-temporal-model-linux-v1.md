# Temporal-prefix cache: bounded Linux model qualification

The frozen Linux plan passed unchanged. Both isolated no-run builds and all five model processes exited 0. Expanded and compact original controls, the temporal-prefix candidate, and both repeated controls agree exactly within Linux on all 1,904 canonical and 2,144 mixed tensor identities. The seventeen canonical actual argmaxes, fifteen independent single results, fifteen independent mixed results and three/two/one active-row schedule also agree. Free outputs reached EOS at seventeen, two and six tokens.

All ten warmed full-Runner decode intervals reported zero allocation calls and requested bytes, with one start and one end per interval. These intervals exclude load, preprocessing, prefill, detokenization and result assembly. They do not measure capacity, peak RSS or speed. The run occurred concurrently with other functional work.

Windows/Linux comparison is a separate observation. Literal free outputs, IDs, stops, dimensions, counts and actual argmaxes agree for all five invocations. Intermediate tensor bits differ: 1,846 of 1,904 canonical hashes and 2,066 of 2,144 mixed hashes differ, with identical names, dtypes, shapes and element counts. Full tensor payloads were not retained, so these hashes do not quantify numerical error. No cross-platform tensor-equivalence or new numerical-tolerance claim follows.

The durable receipt is `prefix-temporal-model-linux-v1.json`, SHA256 `5272bcdfdc29391a9f11fc5ee05407b0ad2cea3834db3ae920d7862f5a9fae17`. It rechecks 234 source, input and result files. Primary artifact identities are:

| Artifact | SHA256 |
|---|---|
| Plan | `87410dac06e056a58373ea37d5982355d7b09f2916ec0307389ebdc698cfa8ed` |
| Build | `69432db527ecbb908176093676dd50612967c4e748a25d054fd67de3e822f0f4` |
| Execution | `d6dc402e246c3b295beb6414cc5ba7b5754b130c69b60b4f640139e3496cca0b` |
| Comparison | `670a7eb2ab62c8b8865636db0caf61dca084605afa95cb31967abf5ad2e4831d` |
| Control executable | `90cc356b39a7cdd57d1995d0a7e9e72f4f61637d1b236dbb2bbaa4af965cb0e2` |
| Candidate executable | `5d329ff2201d7a09765161d0de728f2b652d49d184694e7fbb19ebf1ff6a5d5b` |

Builds used separate fresh targets on the D-backed WSL filesystem, Rust 1.92.0, offline/locked dependencies and two compiler jobs. Emitted manifests, source-copy paths, artifact freshness, target containment and distinct executable hashes were checked. Source/toolchain/build evidence is preserved, but it is not a hermetic-build attestation. Runtime used four CPU threads and disabled CUDA. The initial ordinary-sandbox WSL access denial occurred before any execution-start record; the authorized elevated launch executed the frozen plan once.

This qualifies the isolated candidate's CPU layout equivalence for the fixed smoke and three-input mixed cases on each tested platform. It does not close GPU hidden-stage or natural-corpus quality gates, establish performance, qualify arbitrary continuation shapes, or change production defaults.
