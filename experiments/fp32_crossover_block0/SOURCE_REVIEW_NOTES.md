# Prospective block0 observation contract

Independent source review and runtime release are pending. This note is part
of the prospective source closure; do not edit it after preparation.

This version adapts only the preserved layer7 diagnostic into one native
block0 crossover. It uses original complete embeddings, fourteen stages,
five original controls on each platform, and a total of three native blocks.
No downstream fixed-coordinate substitute, suffix, generation or arithmetic
variant is present. Original telescope evidence is pinned as context only.

Rust observations retain the original operation body by a mechanical removal
guard; the source-only GPU guard compares its full native setup, hooks, loop,
control ordering and cleanup to the accepted layer7 source. Scope labels and
block index are the only native-fragment changes, apart from an entry-bit
check. Source/receipt hashing and added synchronization are observational
changes and cannot establish unobserved full-graph equivalence.

The exporter now gates CUDA initialization on a bound accepted CPU execution
receipt, exact control claims, compiled-source/build/toolchain and artifact
identities. The comparer repeats the control tests against actual arrays.
CPU source and toolchain remain pinned to the original 1.92 behavior even
while the live project upgrades to 1.94. All stages retain strict FP32.

Reviews must check: complete entry identities; ten exact original controls;
all 42 arrays; source/input windows; the 1+2 block budget; native mask/cache/
position order; no implicit phase chaining; and conditional signed FP64
accounting. Control failure must preserve rejection with no retry. No
relaxation of the ten frozen model-stage failures is permitted.
