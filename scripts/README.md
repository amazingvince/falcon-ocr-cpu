# Frozen GPU-reference closure

These files are hashed by path in the receipts under `reference/` (the strict FP32 GPU export, the corpus contract and the reference-run identity), so they stay here unchanged. Do not edit or move them. The protocol they implement, and the tools that grew around them, are described in [research/gpu-reference/README.md](../research/gpu-reference/README.md); the product-path build and model helpers live in [tools/](../tools/).
