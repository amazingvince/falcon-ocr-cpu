The observed GPU W2 scratch supports a useful, bounded numerical comparison: **one ascending-slot FP32 fold reproduces all 110,592 original outputs exactly**, with zero bit mismatches and zero maximum/RMS error. This establishes output equivalence for these saved inputs, not the GPU's instruction or reduction order.

The direct-call capture is `artifacts/reference/linear-owned-workspace-fp32-v1/report.json`, SHA `697b717ac9949c1f90bf94eaca8f074164ec40d10f7917ca8bd4ecf3c5a7a7f4`. The real control and all three prospective coded one-sparse probes passed the exact-output, executed-algorithm and ordered-kernel gates. They use the original full matrix dimensions and a caller-owned, initialized 32 MiB workspace. No retry, layout search, private API or unowned-memory read was used.

An independent CPU reader rehashed all 58 capture artifacts before and after analysis, checked the archived source bytes, and joined the control input, weight and expected output directly to their original pinned safetensors tensors. It independently checked every basis channel/row/partial location. Under the single declared layout—workspace offset zero, FP32 little endian [14,144,768]—all basis values agree. The observed memberships are contiguous `[165*s,165*(s+1))` for slots s=0..12 and `[2145,2304)` for slot13. These boundaries come from the complete basis observations, not a split-count or chunk-size sweep. They remain conditional on the tested scratch interpretation; distinct row/channel products can collide, and this is not a public cuBLAS workspace ABI.

The CPU replay uses an FP32 +0 matrix and fourteen explicitly ordered `np.add(..., dtype=float32, out=...)` operations, slots0 through13. It performs no BLAS call or alternative fold. Its preserved source, 14-part real scratch extraction, full folded output and comparisons are in `artifacts/diagnostics/w2-observed-partial-fold-v1`. The report SHA is `9c7166386f22d3b1f70c8b9a5aad09e6458dd7f7f0f1e2c22a7d2af97c1dae14`; the extracted partials SHA is `d26b4fb62f6059eddaa54054c9dd41b9de9ab8187b5d149f1650f2615ef207c4`. The folded output SHA `aa9c02dae08179baa18fe0bc90086d94a9e82b759c6eb398010f3a8e557c9698` equals the original GPU output bytes.

The next prepared diagnostic is the isolated package `experiments/linear/observed_partition_probe`. It gathers exactly those fixed K ranges into contiguous FP32 rows, uses the same gemm0.19 argument sequence as the production prefill branch, and saves every CPU partial in [14,144,768], an unsplit GEMM control, and a separate ascending fold. Each partial is compared directly with the observed GPU scratch. It uses at most four threads, does not time the operator and does not modify the runner. All 67 registry dependency versions/checksums match the existing production lock. The capture driver preserves source/lock/compiler flags/build logs and exact binary identity; three bounded Rust tests check range coverage, gather bits and GEMM orientation against an independent exact dot.

Preprocessing independently reviewed the Rust probe and its capture driver without a remaining concrete finding. Root owns native Windows/WSL build, tests and execution; this note makes no claim about their results. A full-graph intervention must wait for the isolated partial comparisons. The old `reference/linear-isolated-probe.json` has only summary statistics for this same input, not preserved CPU output bytes; it can provide a secondary comparison but cannot prove an unsplit bitwise baseline match.
