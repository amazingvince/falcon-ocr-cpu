# Prospective layer-7 observation contract

Independent source review is pending. This document is part of the source
closure and must not be edited after preparation. A separate review receipt
will bind the final reviewed sources and describe any limitations.

Compared with the earlier layer-8/9 experiment, this version changes only the
bounded scope and versioned paths: layer-6 entry, layer-7 only, five original
controls per platform, 14 stage captures, fixed endpoint row112/channel249.
The Rust adapter has a mechanical observer-removal equality guard. GPU hooks
pass through native functions; exact original-state controls are mandatory.
New runtime/environment checks require OMP/MKL 8 before GPU initialization.
No saved input, original arithmetic source, policy or historical report changes.
