The existing `cuda_shaped_sum` has the same width-768 reduction structure as the pinned CUDA source. No source-level change to that tree or replacement of division by reciprocal multiplication is justified. The unresolved parts are the actual compiled FMA/division instructions and CUDA's reciprocal-square-root implementation.

The read-only capture used `/home/amazi/falcon-ocr-rust-reference/.venv/bin/python`: Torch `2.11.0+cu130`, declared git revision `70d99e998b4955e0049d13a98d77ae1b14db1f45`, CUDA build version `13.0`. CUDA remained uninitialized before and after. [Captured identity and sources](C:/Users/amazi/Documents/ChatGPT/falcon-ocr/artifacts/diagnostics/rms-cuda-source-v1/identity.json) have identity SHA-256 `ce5aad71feac3f87b83dd58f93038acc19a1e68f1ab00f39bef60bcd8d2b725b`; the unmodified CUDA source download is `6bc37b0cbeb94b534668c6c34b91067acf085ec7e4276a2776dec9351466e066`. This binds installed version/header evidence to its declared source revision, not a disassembled device binary.

For contiguous FP32 rows with 16-byte-aligned buffers, width 768 selects the vectorized RMS specialization. The following source comparison concerns that path; the alignment fallback has different arithmetic. [Exact-revision CUDA implementation](https://github.com/pytorch/pytorch/blob/70d99e998b4955e0049d13a98d77ae1b14db1f45/aten/src/ATen/native/cuda/layer_norm_kernel.cu).

| Operation | CUDA source and diagnostic comparison |
|---|---|
| Loads | Four adjacent values per vector. Thread `t` consumes channels `4t..4t+3`, then another vector 512 channels later when present. Threads 0–63 consume eight values; 64–127 consume four. Matches the diagnostic. |
| Local accumulation | CUDA expresses multiply plus accumulated sum; Rust explicitly uses `mul_add`. Same value order, conditional equivalence of rounding. |
| Warp reduction | Downward offsets 16, 8, 4, 2, 1. Rust updates only lanes needed for lane zero; its dependency tree matches. |
| Warp combination | Four warp totals combine as `(w0+w2)+(w1+w3)`. Matches Rust. |
| Mean square | CUDA divides by a floating-point width; Rust divides by 768. Neither source explicitly multiplies by a rounded reciprocal. |
| Epsilon | Addition follows division. The implicit FP32 epsilon matches `f32::EPSILON`. |

The installed [thread constants](C:/Users/amazi/Documents/ChatGPT/falcon-ocr/artifacts/diagnostics/rms-cuda-source-v1/installed/include/ATen/native/cuda/thread_constants.h) establish four 32-lane warps, hence 128 threads. The packaged math, thread, block-reduction and accumulator headers contain their exact upstream text inside packaging guards; their separate byte hashes are preserved. [Generic dispatch](https://github.com/pytorch/pytorch/blob/70d99e998b4955e0049d13a98d77ae1b14db1f45/aten/src/ATen/native/layer_norm.cpp) selects fused RMSNorm for the ordinary real-valued, matching-dtype layout; channels-last/complex/mismatched-weight cases have separate fallbacks.

The installed [math compatibility header](C:/Users/amazi/Documents/ChatGPT/falcon-ocr/artifacts/diagnostics/rms-cuda-source-v1/installed/include/c10/cuda/CUDAMathCompat.h) maps the FP32 reciprocal square root to CUDA `rsqrtf`. CUDA 13 documents up to **2 ULP** error relative to the correctly rounded FP32 result. Therefore neither Rust's two FP32 steps nor its FP64 computation followed by an FP32 cast is a guaranteed emulation. The latter is a higher-precision diagnostic, not observed CUDA behavior. This documented bound is **not** a proposed parity tolerance. [CUDA 13 accuracy table](https://docs.nvidia.com/cuda/archive/13.0.0/cuda-c-programming-guide/index.html#mathematical-functions-appendix), [separate round-to-nearest intrinsic](https://docs.nvidia.com/cuda/archive/13.0.0/cuda-math-api/cuda_math_api/group__CUDA__MATH__INTRINSIC__SINGLE.html).

NVCC normally enables multiply-add contraction and precise division, while preserving subnormals. Those defaults support the diagnostic's FMA assumption but do not prove this wheel's emitted instructions. Installed build metadata records architecture targets and host flags, not the complete device optimization flags. No PTX/SASS was obtained; `cuobjdump` and `nvdisasm` are absent from the inspected PATH. Exact load lowering, contraction, division lowering and `rsqrtf` instruction sequence remain unverified. [NVCC 13 options](https://docs.nvidia.com/cuda/archive/13.0.0/cuda-compiler-driver-nvcc/index.html#fmad).

The queued actual-rstd export and same-argument GPU `rsqrt` replay are the next discriminating evidence. Replaying both saved CPU variance bit sets can isolate the function's contribution without pretending those values are the native GPU variance. Native rstd alone cannot uniquely determine the underlying sum. No candidate sweep, production edit, GPU work or tolerance change was performed for this note.
