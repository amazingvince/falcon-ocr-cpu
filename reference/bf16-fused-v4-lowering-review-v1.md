The saved rejected BF16 v4 capture contains a specific instrumentation-induced arithmetic change in the masked-loop denominator. The capture remains rejected: all three raw BF16 arrays match, but natural LSE differs in 7/9/12 entries and log2 LSE in 7/11/13 entries. Original outputs still match the frozen fixture. Raw agreement does not waive the LSE gate.

In `original-0.ttgir:269`, the probability sum reduces an MMA-layout tensor. In `instrumented-0.ttgir:544`, the corresponding sum instead reduces a blocked layout with one row per thread. The result feeds the actual `l_i` recurrence, is carried out of the masked loop, and later reaches the final LSE. This is not just an observer-only duplicate.

The PTX makes the difference concrete. Original lines6358-6460 use local partial sums and XOR2/XOR1 lane shuffles before the denominator FMA. Instrumented lines12328-12390 instead contain 63 consecutive additions of64 values in one thread; line12397 consumes this sum in the denominator FMA. Its source location points to the added `sum_unrounded_probabilities_f32` store. The full-block loop retains an MMA reduction. The separate natural-LSE pointwise PTX/CUBIN/IR files are byte-identical between the two programs, so this is not a changed log2-to-natural conversion kernel.

One justified correction target is to prevent the debug-only `tl.sum(p,1)` observation consumer from selecting/reusing this changed reduction layout in the live denominator path. No guaranteed source-level fix is established by this read-only audit. If a single constrained observer correction is pursued, its generated reduction layout and addition/shuffle structure should first retain the original MMA arithmetic; it must then pass every unchanged complete-output bit gate. Removing one observer expression alone has not been shown sufficient.

The evidence proves a relevant arithmetic perturbation, not a unique explanation for every LSE mismatch. Rejected intermediate values remain unqualified as native GPU evidence. No source edits, compilation, GPU execution, new fixtures or tolerance changes were performed. The companion JSON binds the reviewed source/PTX/IR identities and records exact evidence locations.
